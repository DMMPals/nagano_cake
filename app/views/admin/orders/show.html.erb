<h2>注文履歴詳細</h2>
<table>
  <thead>
    <tr>
      <th>購入者</th>
      <th><%= link_to admin_customer_path(@order) do %>
            <%= @order.customer.last_name %><%= @order.customer.first_name %>
          <% end %></th>
      <th>注文日</th>
      <th><% @order.created_at %></th>
      <th>配送先</th>
      <th><%= @order.order_address %></th>
      <th>支払い方法</th>
      <th><%= @order.order_status %></th>
      <th>注文ステータス</th>
        <th><%= form_with model: @order, url: admin_order_path, method: :patch, local: true do |f| %>
              <%= f.select :order_status, Order.order_statuses.keys, class: "order_status" %>
              <%= f.submit "更新" , class: "btn btn-outline-success" %>
            <% end %></th>
    </tr>
  </thead>

  <tbody>

  　　　<tr>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
　　<% end %>
  </tbody>